specVersion: "v1"
serviceId: "kural"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "kural-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
